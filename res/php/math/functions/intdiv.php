<?php

/**
 * Integer division
 *
 * @phpstub
 *
 * @param integer $numerator
 * @param integer $divisor
 *
 * @return integer The integer division of  by .
 *                 If  is zero, it throws an  and returns false.
 *                 If the  is LONG_MIN (- - 1) and the
 *                 is -1, it returns zero.
 */
function intdiv($numerator, $divisor)
{
}
<?php

/**
 * Filters elements of an array using a callback function
 *
 * @phpstub
 *
 * @param array $array
 * @param callable $callback
 * @param int $flag
 *
 * @return array Returns the filtered array.
 */
function array_filter($array, $callback = NULL, $flag = false)
{
}
<?php

/**
 * Replaces elements from passed arrays into the first array
 *
 * @phpstub-variable-parameters
 * @phpstub
 *
 * @param array $array1
 * @param array $array2
 * @param array $_
 *
 * @return array Returns an ``array``, or null if an error occurs.
 */
function array_replace($array1, $array2, $_ = array())
{
}
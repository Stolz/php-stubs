<?php

/**
 * Import variables into the current symbol table from an array
 *
 * @phpstub
 *
 * @param array $array
 * @param int $flags
 * @param string $prefix
 *
 * @return int Returns the number of variables successfully imported into the symbol
 *             table.
 */
function extract(&$array, $flags = false, $prefix = NULL)
{
}
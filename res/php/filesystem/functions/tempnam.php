<?php

/**
 * Create file with unique file name
 *
 * @phpstub
 *
 * @param string $dir
 * @param string $prefix
 *
 * @return string Returns the new temporary filename (with path), or false on
 *                failure.
 */
function tempnam($dir, $prefix)
{
}